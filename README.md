# simple-backend-file-system
A beginner backend project built with Node.js and Express.js that allows users to create tasks through a form. Each task is saved as a file on the server and can be read later. The frontend is built using Tailwind CSS, EJS, and Vanilla JavaScript to understand the full workflow between frontend and backend.
